import AnalyticsAdapter from "workarea/models/analytics"

export default class <%= adapter_name %> extends AnalyticsAdapter {
  send(event, payload) {
    // Send data to 3rd party. Use `this.config` to access configuration
    // for this analytics service (`config.adapters[ADAPTER_NAME]`)
  }

  // Events to send:
  pageView() {
    this.send('pageView')
  }

  categoryView(payload) {
    this.send('categoryView', payload)
  }
}
