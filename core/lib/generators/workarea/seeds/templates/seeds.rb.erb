module Workarea
  class <%= class_name %>Seeds
    def perform
      # Create the data necessary for this application to run or useful for
      # getting started with this implementation of Workarea.
      #
      # Examples:
      #
      #   Catalog::Product.create!(name: 'Foo')
      #
      #   tax = Tax::Category.create!(name: 'Foo', code: '001')
      #   tax.rates.create!(percentage: 0.07)
    end
  end
end
