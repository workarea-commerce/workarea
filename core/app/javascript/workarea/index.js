import Engine from "./engine"
import Application from "./application"
import config from "./config"

/**
 * Main export of the new Workarea JS. This is just a convenience method
 * for everything.
 */
export default { Engine, Application, config }
