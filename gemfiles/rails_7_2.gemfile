# frozen_string_literal: true

eval_gemfile File.expand_path('../Gemfile', __dir__)

gem 'rails', '~> 7.2.0'

# Mongoid 7.4.x caps ActiveModel < 7.1, blocking Rails 7.2+ resolution.
# Override to Mongoid 8.x for this appraisal until the core gemspec is updated.
gem 'mongoid', '~> 8.1'
